
name: Merge other brach to master
on:
  push:
    branches:
      - wayes
      - mizan
jobs:
  merge-branch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master

      - name: Merge staging -> main
        uses: Bit-Apps-Pro/bit-integrations-doc@main
        with:
          type: now
          target_branch: main
          github_token: ${{ github.token }}
